// Theme Configuration
export const theme = {
  colors: {
    bgBase: '#062419',        // Main window background (Deep forest green)
    bgSurface: '#0a3625',     // Sidebar, cards, recent history, inputs
    bgOverlay: '#104d36',     // Dropdowns, modals, tooltips, active states
    textPrimary: '#f1f5f9',   // Main titles, active labels, code
    textSecondary: '#94a3b8', // Muted metadata, descriptions, placeholders
    accent: '#22c55e',        // Vivid green for success, active tabs, highlights
    accentMuted: '#15803d',   // Border lines, disabled states
    volt: '#4ade80',          // High-contrast AI/Copilot components
  },
  radius: { base: '8px', pill: '9999px' },
  transition: 'background-color 0.2s ease, border-color 0.2s ease',
};
